学习目标
掌握字典的常用操作

课程内容
a.字典的应用场景
b.创建字典
c.访问字典
d.修改字典
e.删除字典
f.补充

a.字典的应用场景
	1.前后端的数据传输
	2.浏览器提交数据给服务器
	3.接口开发

b.创建字典
	字典是python中的映射类型
	由键值对组成  name:'gaga'
	一般字符串或者数字作为键
	dic={'name':'niufuren',age:18}
	dic={'age':18}
c.访问字典
	dic={'name':'heygor','age':18}
	print(dic)
	print(dic['name'])
d.修改字典
	dic={'name':'gaga','age':18}
	print(dic)
	dic['name']='8jie'
	print(dic)
	dic['age']=38
	print(dic)
e.删除字典
	1.del
	删除制定元素(键值对)
	删除整个字典(不能访问)
	dic={'name':'heygor','QQ':110}
	print(dic)
	del dic['QQ']
	print(dic)
	del dic
	print(dic)
	2.clear
	清空字典
	dic={'name':'heygor','QQ':110}
	print(dic)
	dic.clear()
	print(dic)

f.补充
	1.keys(键)
	dic={'name':'5kong','age':18}
	print(dic.keys())
	for i in dic.keys():
        	print(i)
	2.values(值)
	print(dic.values())
	for i in dic.values():
        	print(i)
	3.items(键和值)
	for i,j in dic.items():
	        print(i,j)


~                   


	
~              
